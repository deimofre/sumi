# 追加実装指示書: 紙モード(毛細管にじみモデル)の導入

対象: 墨流体プロジェクト(既存の WebGL2 流体シミュレーション実装)
目的: 既存の「水面に墨を流す」表現(以下 **fluid モード**)を残したまま、「紙に筆で書く」表現(以下 **paper モード**)を追加し、実行時に切り替えられるようにする。

---

## 0. 実装前に必ず行うこと

1. 既存コードの構成を調査し、以下を報告してから着手する。
   - シミュレーション各パス(splat / advect / pressure など)がどのファイル・関数にあるか
   - ポインタ入力の処理位置と、ストロークに関する状態(方向・速度・筆圧・入り抜き)がどこで持たれているか
   - 表示(合成)シェーダーの位置と、それが読んでいるテクスチャ
   - 紙テクスチャ(プロシージャル or 画像)の生成・読込位置
2. 報告に基づき「共通化する部分」と「モード固有の部分」の切り分け案を提示し、承認を得てから実装に入る。
3. **既存 fluid モードのコードは原則変更しない。** 変更が必要な場合は理由を明記し、承認を得る。

---

## 1. 要件

### 1.1 モード切替
- `mode: 'fluid' | 'paper'` を持つ。初期値は既存の挙動(fluid)。
- 切替手段: 画面上のトグルUI、および URL パラメータ `?mode=paper`。
- 切替時はシミュレーション状態をクリアし、不要なFBOは解放する。
- fluid モードの見た目・挙動は、切替導入後も**完全に一致**すること(回帰確認を行う)。

### 1.2 共通化する層
以下は両モードで共有し、モード固有コードに複製しない。
- **入力層**: ポインタ → ストロークサンプル列。各サンプルは `位置, 方向, 速度, 筆圧(pressure), 傾き(tiltX/tiltY または altitude/azimuth), ストローク累積長, 入り(entry)係数, 抜き(exit)処理` を持つ。既存の入り・抜きロジックはここに置く。
- **紙テクスチャ**: 画像があれば読み込み、無ければプロシージャル生成にフォールバック。
- **表示合成の色設計**: 濃墨・薄墨・湿り艶・和紙・落款の色ルール。

### 1.3 非目標
- 筆の毛一本ごとの3Dシミュレーション
- 複数色・水彩
- Undo / 保存(別タスク)

---

## 2. paper モードの物理モデル

参考: Chu & Tai, "MoXi: Real-Time Ink Dispersion in Absorbent Paper" (SIGGRAPH 2005) / Curtis et al., "Computer-Generated Watercolor" (SIGGRAPH 1997)。数式を厳密に再現する必要はなく、以下の3層構造と現象が再現できればよい。

### 2.1 状態(1枚の RGBA16F テクスチャにパック)
| ch | 名称 | 意味 |
|---|---|---|
| R | **W** 水量 | その点の紙が含んでいる水の量 |
| G | **P** 移動顔料 | 水に乗って動く墨 |
| B | **F** 固定顔料 | 乾いて定着した墨。以後変化しない |
| A | 湿り年齢 | 艶表現用(任意)。W が増えた時に1、以後減衰 |

解像度: 短辺 1024(スマホ)〜2048(iPad/PC)。ピンポン2枚。

### 2.2 紙の入力(画像またはプロシージャル)
| ch | 名称 | 用途 |
|---|---|---|
| RGB | albedo (sRGB) | 表示 |
| A | height (linear) | 掠れ判定 / 保水容量 / 顔料の沈殿ムラ |
| 任意2枚目 RG | 繊維方向ベクトル | にじみの異方性 |
| 任意2枚目 B | 多孔率 | 局所的な染みやすさ |

画像が無い場合は現在のプロシージャル紙から height と繊維方向を生成する。

### 2.3 毎フレームのパス(順序固定)
1. **Deposit(筆の載墨)** — ストロークサンプルごとに実行
   - 筆の接地面(フットプリント)内で `W += water`, `P += pigment`
   - 接地判定に紙の height を使う: `contact = smoothstep(threshold, threshold + softness, height)`。`threshold` は筆圧が低いほど・筆の墨残量が少ないほど**上がる** → 高い繊維にしか墨が付かず、掠れの筋が紙の凹凸と一致する。
2. **Diffuse(毛細管拡散)**
   - W を隣接4〜8点へ流す。流量 = `k × (W_i − W_j) × 透過率(j)`。透過率は繊維方向と多孔率から算出(繊維方向と平行なら大、直交なら小)。
   - **ピン止め**: W がしきい値以下の点からは流出しない(紙に吸われて止まる)。これが「にじみが自然に止まる」原理。
   - **飽和**: W の上限を height/多孔率から決める(容量以上は受け取らない)。
3. **Transport(顔料の移動)**
   - P を水の流束(前パスで計算した各方向の流量)に沿って運ぶ。微小な等方拡散を足す。
4. **Evaporate & Fix(蒸発と定着)**
   - `W -= evap × dt`。W が薄い場所ほど蒸発が速い(縁から乾く)。
   - `F += P × fixRate × (1 − W / Wmax)`, `P -= 同量`。乾いた場所から順に定着する。
   - 期待される創発現象: 縁が先に乾く → 内側から縁へ水が流れる → P が縁に運ばれ F として固定 → **にじみの縁が濃くなる**(コーヒーリング)。これはノイズで偽装せず、上記の物理から出ること。
5. **Display(表示)** — 共通層の色ルールを適用
   - 濃度 = F + P。W と湿り年齢で艶。height で沈殿ムラ。
   - fluid モードの表示シェーダーとは別ファイルにしてよいが、色定数は共通定義を参照する。

### 2.4 筆モデル(入力層とpaperモードの境界)
- **フットプリント**: 筆圧で半径、傾きで楕円化(長軸は傾き方向)。傾き情報が無い端末では円。
- **毛の割れ**: 筆圧が低い/墨残量が少ないとき、フットプリントを複数の細い接地面に分割する(乱数シードはストロークごとに固定)。
- **墨残量 (inkRemaining)**: ストローク開始時 1.0。累積長 × 筆圧で減少。`pigment` と `water` の供給量に比例。0 に近づくほど掠れ判定の threshold が上がる。
- **留まり**: 特別処理は不要。同じ場所へ供給が続けば W が飽和し周囲へ拡散し続けることで自然に出る。既存の「hold」ロジックは供給の継続として扱う。
- **入り・抜き**: 既存ロジックを入力層に移し、両モードで共有。抜きの外挿サンプルにも inkRemaining と筆圧の減衰を適用する。

---

## 3. パラメータ

すべて一つの設定オブジェクトにまとめ、デバッグパネル(画面上、開閉可)からリアルタイムに変更できること。**作者はコードを書かずパラメータ調整のみ行う前提。**

| 分類 | パラメータ | 初期値の目安 |
|---|---|---|
| 拡散 | 拡散係数 k | 0.2〜0.4 /frame |
| 拡散 | 異方性(繊維方向の効き) | 0.5 |
| 拡散 | ピン止めしきい値 | 0.02 |
| 拡散 | 飽和容量の基準 | 1.0 |
| 蒸発 | 蒸発速度 | 半減期 3〜6 秒相当 |
| 定着 | fixRate | 0.5 /s |
| 筆 | 基準半径・筆圧カーブ | 短辺の 1〜2% |
| 筆 | 墨容量(1ストロークで書ける長さ) | 短辺の 1.5 倍 |
| 筆 | 掠れ threshold カーブ | 墨残量 1→0 で 0.3→0.8 |
| 筆 | 毛の割れ開始点 | 墨残量 0.4 |
| 表示 | 縁の濃さ、沈殿ムラ強度、艶 | 既存値を引き継ぐ |

プリセット保存(JSON書き出し/読み込み)を用意する。

---

## 4. 実装フェーズと各フェーズの確認方法

各フェーズ完了時に、iPad(Apple Pencil)と iPhone で確認できる状態にする。

**Phase 0** 現状調査と切り分け案の提示(§0)。

**Phase 1** モード骨組み。paper モードは「紙が表示されるだけ」で可。切替UI、URLパラメータ、fluid の回帰確認。

**Phase 2** 3層コア(Deposit / Diffuse / Evaporate & Fix)。筆は既存入力をそのまま使う。
確認: 一点に触れて留まる → 円形ににじみ、時間とともに止まり、縁が濃く固定され、中心はやや薄い。

**Phase 3** 筆モデル(フットプリント、墨残量、height による掠れ)。
確認: 速い払いで筋状の掠れが出て、筋の位置が紙の凹凸と一致している。同じストロークの後半ほど掠れが増える。

**Phase 4** 表示合成の統合(共通色ルール、艶、落款)。

**Phase 5** デバッグパネル、プリセット、パフォーマンス調整。

---

## 5. 受け入れ基準

1. 留まり: 一点で押し続けると円形に広がり、放しても数秒でにじみが止まり、縁が濃く残る。
2. 掠れ: 筋は紙の height と一致し、ノイズによる無関係な筋が無い。
3. 墨切れ: 1ストローク内で墨が減り、終端ほど掠れる。
4. 渦や慣性による流動が一切出ない(紙モードで墨が「流れて戻る」ことは無い)。
5. 定着: 乾いた墨(F)はその後の筆や水で動かない。
6. 性能: iPhone で短辺 1024 のとき 60fps、iPad で短辺 2048 のとき 60fps を目標。
7. 回帰: fluid モードの見た目・挙動が導入前と一致。
8. 紙画像(albedo+height)を差し替えると、掠れとにじみの形が変わることを確認。

---

## 6. 制約

- WebGL2 のまま。外部ライブラリの追加は不可。
- 新規コードは paper モード用のディレクトリ/ファイルに分離し、既存ファイルへの変更は共通層の抽出に限る。
- 半精度 float FBO を前提とし、非対応端末では既存のフォールバック方針に従う。
- 数値の説明コメントを各パラメータに付ける(何を変えると何が変わるか)。
